# Keep the root/input session unpinned so it inherits the model and reasoning
# settings selected by the user or Codex. Legal subagents pin their own models.

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true
